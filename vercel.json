{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/web run build && node scripts/vercel-build.mjs",
  "crons": [
    { "path": "/api/fetch-real-odds", "schedule": "0 5 * * *" },
    { "path": "/api/fetch-results", "schedule": "30 5 * * *" }
  ]
}
